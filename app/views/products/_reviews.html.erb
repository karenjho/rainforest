<p>Reviews for <%= @product.name %></p>

<% @product.reviews.each do |review| %>
  <p>
    <%= review.comment %>
    <br>
    <% if review.user != nil %>
      <em> by <%= review.user.email %></em>
      Added on: <%= review.created_at %>
      <%= link_to "Edit review", edit_product_review_path(review.product_id) %>
    <% end %>
  </p>
<% end %>
